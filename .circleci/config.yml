# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
jobs:
  echo:
    docker:
      - image: node:16
    steps:
      - checkout
      - run: echo "${CIRCLE_WORKFLOW_ID}"
      - run: echo "${CIRCLE_JOB}"
      - run: echo "${CIRCLE_BUILD_NUM}"
      - run: echo "${CIRCLE_BUILD_URL}"
      - run: echo "${CIRCLECI_TOKEN}"
      - run: echo "${CIRCLE_TOKEN}"
      - run: npm install "git+https://github.com/DataDog/datadog-ci.git#397c4b510cae3a9b57b686cb7fcc95513f530c1d"
      - run:
          name: "Custom span"
          command: datadog-ci trace ./customspanschildren.sh
          environment:
            DATADOG_SITE: datad0g.com
workflows:
  echo:
    jobs:
      - echo
