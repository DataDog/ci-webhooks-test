name: Simple Test GitHub Actions
on: [push]

jobs:
  container-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello world!"
      - uses: actions/checkout@v3
      - run: experimental-datadogci.sh
